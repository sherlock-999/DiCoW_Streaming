# Core
numpy
torch
librosa
soundfile

# Whisper backends (install at least one)
faster-whisper          # recommended backend (default)
# whisper-timestamped   # alternative backend

# Transformers / DiCoW model
transformers
pandas

# Sentence tokenizers (optional, for buffer_trimming=sentence)
# mosestokenizer        # for most languages
# wtpsplit              # alternative sentence splitter

# VAD (required for VACOnlineASRProcessor)
# silero-vad is loaded via torch.hub at runtime, no pip install needed
